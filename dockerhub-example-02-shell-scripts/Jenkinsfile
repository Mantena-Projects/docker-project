pipeline {
  agent any
  environment {
    DOCKERHUB_CREDENTIALS = credentials('darinpope-dockerhub')
  }
  stages {
    stage('Login') {
      steps {
        sh ''
      }
    }
    stage('Push') {
      steps {
        sh ''
      }
    }
  }
  post {
    always {
      sh 'docker logout'
    }
  }
}
}
